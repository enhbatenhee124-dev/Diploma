import { createApp } from '../server/app.js'

// ============================================================
// Vercel Serverless-ийн орц
// ============================================================
// Vercel нь `api/` доторх файлыг serverless функц болгон ажиллуулна.
// `vercel.json` дахь rewrite нь `/api/*` бүх хүсэлтийг ЭНЭ файл руу
// чиглүүлж, анхны замыг (`req.url`) хэвээр дамжуулна. Тиймээс Express
// доторх `/api/shifts` гэх мэт зам өөрчлөлтгүй ажиллана.
//
// `createApp()` нь `listen()`-ээс тусдаа байсан нь энд ашиг тусаа өглөө:
// нэг ч мөр өөрчлөхгүйгээр монолит нь serverless дээр ажиллаж байна.
//
// ⚠ Санах ойд түшсэн зүйл (rate limit) нь instance тус бүрд тусдаа болно.
//   Хатуу хязгаар шаардлагатай болбол Redis эсвэл Postgres руу зөөнө —
//   `core/rateLimit.js`-ийн интерфэйс хэвээр үлдэнэ.
// ============================================================

const app = createApp()

export default app
